# Commands

```
yarn create next-app 03-ignews

yarn add typescript @types/react @types/node -D
yarn dev // Next will automatically detect the .tsx files in the project and adds a tsconfig.json and a next-env.d.ts file in the project
```
